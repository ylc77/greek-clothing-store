"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useMemo, useRef, useState } from "react";
import { useCart } from "@/components/cart-provider";
import { CheckoutOperationStore } from "@/lib/checkout-operation";
import type { Language } from "@/lib/i18n";
import type { BusinessSettings } from "@/lib/settings";

export function CheckoutPageClient({ language, settings, legalReady }: { language: Language; settings: BusinessSettings; legalReady: boolean }) {
  const en=language==="en"; const router=useRouter(); const {items,totals,clear}=useCart();
  const operationStore=useRef<CheckoutOperationStore|null>(null);
  const [method,setMethod]=useState<"delivery"|"pickup">(settings.delivery_enabled?"delivery":"pickup");
  const [form,setForm]=useState({name:"",email:"",phone:"",addressLine1:"",city:"",postalCode:"",notes:""});
  const [accepted,setAccepted]=useState(false); const [submitting,setSubmitting]=useState(false); const [message,setMessage]=useState("");
  const shipping=method==="pickup"?0:settings.free_shipping_threshold!==null&&totals.subtotal>=settings.free_shipping_threshold?0:settings.shipping_fee;
  const fingerprint=useMemo(()=>JSON.stringify({method,form,items:items.map(item=>({productSku:item.productSku,size:item.size,color:item.color,quantity:item.quantity})),accepted}),[accepted,form,items,method]);

  async function submit(){
    if(!accepted){setMessage(en?"Please accept the Terms of Sale and Privacy Policy.":"Αποδεχθείτε τους Όρους Πώλησης και την Πολιτική Απορρήτου.");return;}
    if(items.length===0||!legalReady||!settings.online_store_enabled){setMessage(en?"Online checkout is not available.":"Η online ολοκλήρωση παραγγελίας δεν είναι διαθέσιμη.");return;}
    setSubmitting(true);setMessage("");
    try{
      operationStore.current ||= new CheckoutOperationStore(window.sessionStorage);
      const operation=operationStore.current.getOrCreate(fingerprint);
      const response=await fetch("/api/orders",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({operationId:operation.id,accessToken:operation.accessToken,fulfillmentMethod:method,customer:form,items:items.map(item=>({productSku:item.productSku,size:item.size,color:item.color,quantity:item.quantity})),locale:language,legalAccepted:true})});
      const data=await response.json();
      if(!response.ok)throw new Error(data.error||(en?"Order failed.":"Η παραγγελία απέτυχε."));
      const number=String(data.order?.orderNumber||"");
      window.localStorage.setItem(`clothing-store:order-access:${number}`,operation.accessToken);
      operationStore.current.complete(operation.id); clear();
      router.push(`/order/${encodeURIComponent(number)}${en?"?lang=en":""}`);
    }catch(error){setMessage(error instanceof Error?error.message:(en?"Order failed.":"Η παραγγελία απέτυχε."));}
    finally{setSubmitting(false);}
  }

  if(items.length===0)return <section className="ui-container py-16 text-center"><h1 className="text-3xl font-black">{en?"Your cart is empty":"Το καλάθι σας είναι άδειο"}</h1><Link className="ui-button-primary mt-6" href={en?"/?lang=en":"/"}>{en?"Continue shopping":"Συνέχεια αγορών"}</Link></section>;
  return <section className="ui-container py-8 sm:py-12"><div className="mb-8"><p className="ui-kicker">{en?"Secure order":"Ασφαλής παραγγελία"}</p><h1 className="mt-2 text-3xl font-black sm:text-4xl">{en?"Checkout":"Ολοκλήρωση παραγγελίας"}</h1></div><div className="grid gap-6 lg:grid-cols-[minmax(0,1fr)_22rem]"><div className="space-y-5"><div className="rounded-3xl border border-stone-200 bg-white p-5"><h2 className="text-lg font-black">{en?"Delivery method":"Τρόπος παραλαβής"}</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">{settings.delivery_enabled?<button className={`rounded-2xl border p-4 text-left ${method==="delivery"?"border-ink bg-stone-50":"border-stone-200"}`} onClick={()=>setMethod("delivery")} type="button"><b>{en?"Cash on delivery":"Αντικαταβολή"}</b><span className="mt-1 block text-xs text-stone-500">{en?settings.delivery_instructions_en:settings.delivery_instructions_gr}</span></button>:null}{settings.pickup_enabled?<button className={`rounded-2xl border p-4 text-left ${method==="pickup"?"border-ink bg-stone-50":"border-stone-200"}`} onClick={()=>setMethod("pickup")} type="button"><b>{en?"Store pickup":"Παραλαβή από το κατάστημα"}</b><span className="mt-1 block text-xs text-stone-500">{en?settings.pickup_instructions_en:settings.pickup_instructions_gr}</span></button>:null}</div></div><div className="rounded-3xl border border-stone-200 bg-white p-5"><h2 className="text-lg font-black">{en?"Contact details":"Στοιχεία επικοινωνίας"}</h2><div className="mt-4 grid gap-4 sm:grid-cols-2">{[["name",en?"Full name":"Ονοματεπώνυμο"],["email","Email"],["phone",en?"Phone":"Τηλέφωνο"]].map(([key,label])=><label className="text-sm font-bold" key={key}>{label}<input className="input mt-2" onChange={event=>setForm(current=>({...current,[key]:event.target.value}))} value={form[key as keyof typeof form]}/></label>)}</div>{method==="delivery"?<div className="mt-4 grid gap-4 sm:grid-cols-2">{[["addressLine1",en?"Address":"Διεύθυνση"],["city",en?"City":"Πόλη"],["postalCode",en?"Postal code":"Ταχυδρομικός κώδικας"]].map(([key,label])=><label className="text-sm font-bold" key={key}>{label}<input className="input mt-2" onChange={event=>setForm(current=>({...current,[key]:event.target.value}))} value={form[key as keyof typeof form]}/></label>)}</div>:<p className="mt-4 rounded-2xl bg-stone-50 p-4 text-sm text-stone-600">{settings.address}</p>}<label className="mt-4 block text-sm font-bold">{en?"Order notes (optional)":"Σημειώσεις (προαιρετικά)"}<textarea className="input mt-2 min-h-24" onChange={event=>setForm(current=>({...current,notes:event.target.value}))} value={form.notes}/></label></div><label className="flex items-start gap-3 rounded-2xl border border-stone-200 bg-white p-4 text-sm leading-6"><input checked={accepted} className="mt-1" onChange={event=>setAccepted(event.target.checked)} type="checkbox"/><span>{en?"I accept the":"Αποδέχομαι τους"} <Link className="font-bold underline" href={en?"/terms-of-service?lang=en":"/terms-of-service"}>{en?"Terms of Sale":"Όρους Πώλησης"}</Link> {en?"and the":"και την"} <Link className="font-bold underline" href={en?"/privacy-policy?lang=en":"/privacy-policy"}>{en?"Privacy Policy":"Πολιτική Απορρήτου"}</Link>.</span></label>{message?<p className="rounded-2xl border border-red-200 bg-red-50 p-4 text-sm font-bold text-red-700">{message}</p>:null}</div><aside className="h-fit rounded-3xl border border-stone-200 bg-white p-5 lg:sticky lg:top-28"><h2 className="text-lg font-black">{en?"Order summary":"Σύνοψη"}</h2><div className="mt-4 space-y-2 text-sm">{items.map(item=><div className="flex justify-between gap-3" key={`${item.productSku}-${item.size}-${item.color}`}><span className="min-w-0 truncate">{item.quantity}× {en?item.nameEn:item.nameGr} · {item.size}</span><b>€{(item.quantity*item.unitPrice).toFixed(2)}</b></div>)}</div><div className="mt-4 space-y-2 border-t pt-4 text-sm"><div className="flex justify-between"><span>{en?"Subtotal":"Υποσύνολο"}</span><b>€{totals.subtotal.toFixed(2)}</b></div><div className="flex justify-between"><span>{en?"Delivery":"Παράδοση"}</span><b>€{shipping.toFixed(2)}</b></div><div className="flex justify-between pt-2 text-lg"><span className="font-black">{en?"Total":"Σύνολο"}</span><b>€{(totals.subtotal+shipping).toFixed(2)}</b></div></div><button className="mt-6 min-h-13 w-full rounded-full bg-ink px-5 text-sm font-black text-white disabled:bg-stone-300" disabled={submitting||!legalReady||!settings.online_store_enabled} onClick={()=>void submit()} type="button">{submitting?(en?"Placing order…":"Καταχώριση…"):(en?"Place order":"Υποβολή παραγγελίας")}</button><p className="mt-3 text-center text-xs text-stone-500">{method==="delivery"?(en?"Payment in cash on delivery.":"Πληρωμή με μετρητά κατά την παράδοση."):(en?"Payment when collecting in store.":"Πληρωμή κατά την παραλαβή.")}</p></aside></div></section>;
}
